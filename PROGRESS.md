Working on: Rename disciple-tools-plugin-starter to dt-story-sets

The project appears to have been already renamed. I verified that no instances of the old name exist, namespaces are correct, the update checker is correctly configured, and PHP linting passes.

COMPLETE
